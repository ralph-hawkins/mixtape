# Claude-related files stay on this machine only — the public repo is
# just the site itself.
.claude/
CLAUDE.md

# Finder metadata — never useful in a repo.
.DS_Store

# Wrangler's local working state — sign-in details and caches. Never
# belongs in a public repo.
.wrangler/
